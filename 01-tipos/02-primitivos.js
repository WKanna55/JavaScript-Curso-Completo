
/*             TIPOS DE DATOS
  primitivos        |         Referencias
  Number                      Array
  String                      Object
  Boolean                     Function
  Undefined                   Clases
  Null
*/

// Primitivos => se guardan en la ram "Stack"
//                (No varia, se guarda tal cual el valor de la variable)
// Referencia => se guardan en la ram "Heap"
//                (Dinamico, refiere a otra posicion de memoria con el valor de la variable)

let numero = 1; // (tipo) number
let texto = "Hola mundo"; // (tipo) string
let verdadero = true; // (tipo) boolean
let falso = false; // (tipo) boolean
let noDefinido; // undefined => (tipo) variables que no estan asignadas
let undef = undefined; // undefined => (valor)funciona como valor al definirlo
let nulo = null; //(tipo) util cuando no tenemos un valor que definir sin usar undefined
